project_id = "chojxtvzspqqzsmwcbul"

[functions]
verify_jwt = true

# Public SEO/document endpoints are intentionally open so crawlers and
# unauthenticated clients can fetch them directly.
[functions.get-affiliate-config]
verify_jwt = false

# Public HTML prerender endpoint used by social/SEO bots.
[functions.prerender]
verify_jwt = false

# Public XML sitemap endpoint consumed by search engines.
[functions.sitemap]
verify_jwt = false
