# Lovable hosting handles the SPA fallback automatically; this file is kept
# only for portability to hosts that read Netlify-style _redirects.
#
# /sitemap.xml is intentionally NOT proxied to an edge function: the build
# generates the static public/sitemap.xml (scripts/generate-sitemap.mjs),
# which is the single source of truth referenced by robots.txt.
/*             /index.html    200
