# Autonomous Improvement Backlog

This file tracks the highest-impact safe work items identified during autonomous improvement runs.

| Priority | Status    | Discovered issue                                                                                               | Affected files or routes                                                                                                                                                     | User or business impact                                                                  | Proposed solution                                                                                   | Verification requirements                                                                                               |
| -------- | --------- | -------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| P3       | completed | Guide landing pages still could expose copy/share affordances for the example queries and section anchors.     | `src/pages/GuidesIndex.tsx`, `src/pages/GuidePage.tsx`, `/guides`, `/guides/:slug`                                                                                           | Faster sharing and copying would improve retention and make learning loops easier.       | Added copy-query and share actions on guide cards plus copy-link affordances for guide sections.    | Verified with focused guide-page tests plus `npm run typecheck`, `npm run lint`, `npm run build`, and `npm run test`.    |
| P3       | completed | Guide detail pages lacked on-page navigation and recovery-friendly invalid-slug handling.                      | `src/pages/GuidePage.tsx`, `src/pages/__tests__/GuidePage.test.tsx`, `/guides/:slug`                                                                                         | Better navigation and recovery improve mobile usability and reduce dead ends.            | Added on-page jump links, previous/next guide navigation, and stronger invalid-slug recovery UI.    | Verified with focused guide-page tests plus `npm run typecheck`, `npm run lint`, and `npm run build`.                   |
| P1       | completed | Search pages needed stronger first-run guidance and faster query refinement affordances for new users.         | `src/pages/SearchExperience.tsx`, `src/components/SearchRefinementStrip.tsx`, `src/components/SearchRoleGuidancePanel.tsx`, `src/components/EmptyState.tsx`, `/search/:slug` | Lower friction improves search success, retention, and session depth.                    | Added contextual empty-state guidance, quick refine chips, and clearer next actions.                | Verified with targeted component tests plus `npm run typecheck`, `npm run lint`, and `npm run build`.                   |
| P1       | completed | Homepage and shared landing surfaces need clearer conversion paths and indexable content.                      | `src/pages/Index.tsx`, `src/components/HeroSection.tsx`, `/`, `/about`, `/guides`, `/docs`                                                                                   | Better onboarding and SEO can improve acquisition and repeat use.                        | Added quick paths and supporting CTA links for the primary landing surfaces.                        | Verified with homepage component tests plus `npm run typecheck`, `npm run lint`, `npm run build`, and `npm run test`.   |
| P2       | completed | Some browsing routes may have weak scanability on mobile and limited affordances for dense data.               | `src/pages/BrowseDecks.tsx`, `src/pages/BrowseSearches.tsx`, `src/pages/MarketTrends.tsx`, `/decks`, `/browse-searches`, `/market`                                           | Improved mobile usability and scanability increase engagement and task completion.       | Tightened responsive layouts, loading states, and row/card density controls across browse surfaces. | Verified with targeted browse-page tests plus `npm run typecheck`, `npm run lint`, `npm run build`, and `npm run test`. |
| P2       | completed | Analytics and retention instrumentation needed a more actionable operator view of existing funnel data.        | `src/pages/admin-analytics/components/AnalyticsChartsSection.tsx`, `src/pages/admin-analytics/components/ActionableInsightsPanel.tsx`, `/admin/analytics`                    | Faster interpretation helps operators focus on the highest-leverage fixes.               | Added a compact actionable insights panel with direct links to the next best admin actions.         | Verified with focused panel test plus `npm run typecheck`, `npm run lint`, and `npm run build`.                         |
| P3       | completed | Some routes likely have inconsistent empty/error states and could be more resilient.                           | `src/pages/admin-analytics/components/SeoHealthPanel.tsx`, `src/pages/admin-analytics/components/AICostPanel.tsx`                                                            | Better recovery behavior reduces abandonment and support burden.                         | Added resilient loading, empty, and retry states to the SEO health and AI cost admin panels.        | Verified with focused panel tests plus `npm run typecheck`, `npm run lint`, `npm run build`, and `npm run test`.        |
| P3       | completed | The AI SEO page admin route lacked empty-state and retry feedback, making fetch failures feel broken.          | `src/pages/AdminSeoPages.tsx`, `src/pages/__tests__/AdminSeoPages.test.tsx`                                                                                                  | Clear recovery states improve admin trust and reduce time lost to failed operations.     | Added retryable error and empty states plus mobile-friendly admin layout refinements.               | Verified with focused route test plus `npm run typecheck`, `npm run lint`, `npm run build`, and `npm run test`.         |
| P3       | completed | The curated-search admin route lacked explicit recovery states and was awkward on smaller screens.             | `src/pages/AdminCuratedSearches.tsx`, `src/pages/__tests__/AdminCuratedSearches.test.tsx`                                                                                    | Better resilience and mobile ergonomics reduce admin friction and failed operations.     | Added retryable load errors, clearer empty states, and responsive action layout improvements.       | Verified with focused route test plus `npm run typecheck`, `npm run lint`, `npm run build`, and `npm run test`.         |
| P3       | completed | The 404 route was a dead end and did not offer enough recovery paths for lost users.                           | `src/pages/NotFound.tsx`, `src/pages/__tests__/NotFound.test.tsx`                                                                                                            | Better recovery paths reduce abandonment and help users re-enter the product faster.     | Added alternate recovery links to searches, guides, and decks alongside the primary CTA.            | Verified with focused route test plus `npm run typecheck`, `npm run lint`, `npm run build`, and `npm run test`.         |
| P3       | completed | The OAuth consent screen needed clearer loading, recovery, and permission disclosure for sign-in conversion.   | `src/pages/OAuthConsent.tsx`, `src/pages/__tests__/OAuthConsent.test.tsx`                                                                                                    | Better consent UX improves sign-in conversion and reduces abandoned authorization flows. | Reworked the consent flow around query-driven loading, clearer scopes, and recovery links.          | Verified with focused route test plus `npm run typecheck`, `npm run lint`, and `npm run build`.                         |
| P3       | completed | The profile settings route silently hung on failed profile lookups instead of giving users a retry path.       | `src/pages/ProfileSettings.tsx`, `src/pages/__tests__/ProfileSettings.test.tsx`                                                                                              | Clearer account settings failures reduce frustration and support burden.                 | Added a visible load failure state with retry guidance for profile settings.                        | Verified with focused route test plus `npm run typecheck`, `npm run lint`, and `npm run build`.                         |
| P3       | completed | The password reset route did not clearly explain invalid or expired links to users.                            | `src/pages/ResetPassword.tsx`, `src/pages/__tests__/ResetPassword.test.tsx`                                                                                                  | Clearer recovery messaging reduces support friction and abandoned password resets.       | Added invalid-link guidance and a direct path back to sign in or the homepage.                      | Verified with focused route test plus `npm run typecheck`, `npm run lint`, and `npm run build`.                         |
| P3       | completed | The public profile page silently fell back to empty states when profile or deck fetches failed.                | `src/pages/PublicProfile.tsx`, `src/pages/__tests__/PublicProfile.test.tsx`                                                                                                  | Clearer failure states reduce confusion when viewing shared profiles.                    | Added retryable error states and more mobile-friendly deck cards.                                   | Verified with focused route test plus `npm run typecheck`, `npm run lint`, and `npm run build`.                         |
| P2       | completed | Search-progress and SEO/admin panels still contained effect-driven state updates that triggered lint warnings. | `src/components/SearchProgressIndicator.tsx`, `src/components/UnderstoodSummary.tsx`, `src/pages/admin-analytics/components/SeoHealthPanel.tsx`                              | Removing render warnings improves performance and prevents regressions.                  | Removed the query-reset effect from the understood-summary flow by remounting on query changes.     | Verified with `npm run lint`, `npm run typecheck`, `npm run build`, and targeted example-query tests.                   |
| P2       | completed | Edge-function lint/parser issues remain in non-user-facing code paths.                                         | `supabase/functions/semantic-search/index.ts`, `supabase/functions/_shared/logger.ts`, `supabase/functions/mcp/index.ts`                                                     | Cleaner edge-function code lowers deployment risk and future maintenance cost.           | Hardened shared edge logging so circular or exotic payloads no longer break serialization.          | Verified with `npm run lint`, `npm run typecheck`, `npm run build`, full `npm test`, and focused edge-logger tests.     |
| P2       | completed | Mobile browsing routes may be harder to scan than desktop-first layouts.                                       | `src/pages/BrowseDecks.tsx`, `src/pages/BrowseSearches.tsx`, `src/pages/MarketTrends.tsx`, `/decks`, `/browse-searches`, `/market`                                           | Better mobile usability increases engagement and task completion on smaller screens.     | Added mobile-friendly filter rails, jump links, and density controls on browse pages.               | Verified with targeted browse-page tests plus `npm run typecheck`, `npm run lint`, `npm run build`, and `npm run test`. |
