# Autonomous Improvement Backlog

This file tracks the highest-impact safe work items identified during autonomous improvement runs.

| Priority | Status    | Discovered issue                                                                                               | Affected files or routes                                                                                                                                                     | User or business impact                                                            | Proposed solution                                                                    | Verification requirements                                                                                     |
| -------- | --------- | -------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------- |
| P1       | completed | Search pages needed stronger first-run guidance and faster query refinement affordances for new users.         | `src/pages/SearchExperience.tsx`, `src/components/SearchRefinementStrip.tsx`, `src/components/SearchRoleGuidancePanel.tsx`, `src/components/EmptyState.tsx`, `/search/:slug` | Lower friction improves search success, retention, and session depth.              | Added contextual empty-state guidance, quick refine chips, and clearer next actions. | Verified with targeted component tests plus `npm run typecheck`, `npm run lint`, and `npm run build`.         |
| P1       | pending   | Homepage and shared landing surfaces need clearer conversion paths and indexable content.                      | `src/pages/Index.tsx`, `src/components/HeroSection.tsx`, `/`, `/about`, `/guides`, `/docs`                                                                                   | Better onboarding and SEO can improve acquisition and repeat use.                  | Strengthen CTA hierarchy, cross-links, and descriptive copy on primary routes.       | Route smoke check, SEO snapshot review, `npm run typecheck`, `npm run lint`, `npm run build`, `npm run test`. |
| P2       | pending   | Some browsing routes may have weak scanability on mobile and limited affordances for dense data.               | `src/pages/BrowseDecks.tsx`, `src/pages/BrowseSearches.tsx`, `src/pages/MarketTrends.tsx`, `/decks`, `/browse-searches`, `/market`                                           | Improved mobile usability and scanability increase engagement and task completion. | Tighten responsive layouts, loading states, and row/card density controls.           | Mobile viewport review, targeted tests, `npm run typecheck`, `npm run lint`, `npm run build`, `npm run test`. |
| P2       | pending   | Analytics and retention instrumentation may be incomplete for critical funnels.                                | `src/components/RouteTracker.tsx`, `src/pages/AdminAnalytics.tsx`, `src/lib/*analytics*`, `/admin/analytics`                                                                 | Better measurement helps identify drop-offs and growth opportunities.              | Audit events, add missing funnel signals, and document the coverage.                 | Analytics event tests or snapshots, `npm run typecheck`, `npm run lint`, `npm run build`, `npm run test`.     |
| P3       | pending   | Some routes likely have inconsistent empty/error states and could be more resilient.                           | `src/pages/*.tsx`, `src/components/*`, `supabase/functions/*`                                                                                                                | Better recovery behavior reduces abandonment and support burden.                   | Standardize error, loading, and empty states with reusable components.               | Relevant tests for affected routes, `npm run typecheck`, `npm run lint`, `npm run build`, `npm run test`.     |
| P2       | pending   | Search-progress and SEO/admin panels still contained effect-driven state updates that triggered lint warnings. | `src/components/SearchProgressIndicator.tsx`, `src/components/UnderstoodSummary.tsx`, `src/pages/admin-analytics/components/SeoHealthPanel.tsx`                              | Removing render warnings improves performance and prevents regressions.            | Defer state updates to scheduled callbacks and verify no behavioral change.          | `npm run lint`, `npm run typecheck`, `npm run build`, targeted tests where available.                         |
| P2       | pending   | Edge-function lint/parser issues remain in non-user-facing code paths.                                         | `supabase/functions/semantic-search/index.ts`, `supabase/functions/_shared/logger.ts`, `supabase/functions/mcp/index.ts`                                                     | Cleaner edge-function code lowers deployment risk and future maintenance cost.     | Keep resolving syntax/style issues; preserve the unrelated MCP worktree change.      | `npm run lint`, edge-function smoke checks, build verification.                                               |
