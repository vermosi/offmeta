# supabase\functions\semantic-search\pipeline\slots\extract-rarity.ts

- extractRarity · function · L9-L24 — function extractRarity(query: string): { rarity: string | null; remaining: string; }
- extractNegations · function · L26-L61 — function extractNegations(query: string): { excludedTypes: string[]; excludedText: string[]; remaining: string; }
