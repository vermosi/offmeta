# src\components\CompareModal.tsx

- CompareModalProps · interface · L21-L25 — interface CompareModalProps
- StatRow · function · L29-L67 — function StatRow({ label, values, highlight, }: { label: string; values: (string | number | null)[]; highlight?: 'low' | 'high'; })
- CompareModal · function · L69-L225 — function CompareModal({ cards, open, onClose }: CompareModalProps)
